# دليل نظام الإشعارات الذكي
# Smart Notifications System Guide

## 📋 نظرة عامة

نظام الإشعارات الذكي يرسل الإشعارات للأشخاص المناسبين بناءً على أدوارهم الوظيفية، مع إمكانية تخصيص كامل لكل مستخدم.

---

## 👥 الأدوار الوظيفية

### 1. 🔹 إداري (Admin)

**المسؤوليات:**
- متابعة المشاريع والفواتير
- التنسيق مع العملاء للتسليم
- المتابعة المالية

**الإشعارات الافتراضية:**
- ✅ مشروع جديد (NEW_PROJECT)
- ✅ بند جديد (NEW_SUBTASK)
- ✅ تعديل بند (SUBTASK_UPDATE)
- ✅ تحديث الحالة (STATUS_CHANGE) - خاصة:
  - جاهز للتسليم
  - تم التسليم
  - ملغي
  - معلق
- ✅ ملاحظات جديدة (COMMENT_ADDED)
- ✅ مرفقات جديدة (ATTACHMENT_ADDED)

**مناسب لـ:** المدير، المحاسب، المتابع

---

### 2. 🎨 مصمم جرافيك (Designer)

**المسؤوليات:**
- التصميم والتعديلات
- الرد على ملاحظات العملاء

**الإشعارات الافتراضية:**
- ✅ مشروع جديد (NEW_PROJECT)
- ✅ بند جديد (NEW_SUBTASK)
- ✅ تحديث الحالة (STATUS_CHANGE) - فقط:
  - ملاحظات العميل (Has Comments)
  - في انتظار المواد (Awaiting Materials)
  - معلق (On Hold)
  - ملغي (Cancelled)
- ✅ ملاحظة جديدة (COMMENT_ADDED)
- ✅ رد جديد (REPLY_ADDED)
- ✅ تم حل الملاحظة (COMMENT_RESOLVED)
- ✅ مرفقات جديدة (ATTACHMENT_ADDED)

**ملاحظة مهمة:** المصمم لا يستلم إشعارات عن الإجراءات التي يقوم بها هو نفسه.

**مناسب لـ:** المصممين

---

### 3. 🖨️ منسق مطبوعات (Print Manager)

**المسؤوليات:**
- المونتاج والطباعة
- متابعة المواصفات (المقاس ونوع الطباعة)

**الإشعارات الافتراضية:**
- ✅ تحديث الحالة (STATUS_CHANGE) - فقط:
  - تم التصميم (Design Completed)
  - جاهز للمونتاج (Ready for Montage)
  - جاري المونتاج (In Montage)
  - تم المونتاج (Montage Completed)
  - جاري الطباعة (In Printing)
  - جاهز للتسليم (Ready for Delivery)
- ✅ تعديل بند (SUBTASK_UPDATE)
- ✅ تعديل مواصفات (SUBTASK_SPECS_UPDATE)
- ✅ ملاحظات (COMMENT_ADDED) - المتعلقة بالطباعة
- ✅ مرفقات (ATTACHMENT_ADDED)

**مناسب لـ:** مسؤول المطبعة، منسق الإنتاج

---

## 📱 إضافة رقم واتساب جديد

### الخطوات:

1. اذهب إلى **الإعدادات** → تبويب **أرقام الواتساب**
2. اضغط على **إضافة رقم**
3. املأ البيانات:
   - **الاسم**: اسم الشخص (مثال: أحمد المصمم)
   - **النوع**: نوع الحساب (للعرض فقط)
   - **رقم الهاتف**: رقم الواتساب بصيغة دولية (مثال: 201234567890)
   - **API Key**: مفتاح API من CallMeBot
   - **الدور الوظيفي**: اختر الدور المناسب
4. اضغط **حفظ**

### الحصول على API Key من CallMeBot:

1. افتح واتساب على هاتفك
2. أضف رقم CallMeBot: **+34 644 51 44 84**
3. أرسل رسالة: `I allow callmebot to send me messages`
4. ستستلم رسالة تحتوي على API Key الخاص بك
5. انسخ الـ API Key واستخدمه في النظام

---

## ⚙️ تخصيص الإشعارات

كل مستخدم يمكنه تخصيص الإشعارات التي يريد استلامها!

### الخطوات:

1. اذهب إلى **الإعدادات** → تبويب **أرقام الواتساب**
2. اضغط على أيقونة **الإعدادات** (⚙️) بجانب الرقم
3. ستظهر قائمة بكل أنواع الإشعارات
4. فعّل أو عطّل أي نوع حسب رغبتك
5. اضغط **حفظ التفضيلات**

### أنواع الإشعارات المتاحة:

| النوع | الوصف | الأيقونة |
|------|------|---------|
| مشروع جديد | عند إضافة مشروع رئيسي جديد | 📁 |
| بند جديد | عند إضافة بند جديد | ➕ |
| تعديل بند | عند تعديل بند موجود | ✏️ |
| تعديل مواصفات | عند تعديل المقاس أو نوع الطباعة | ⚙️ |
| تغيير الحالة | عند تغيير حالة مهمة أو بند (حسب الدور) | 🔄 |
| ملاحظة جديدة | عند إضافة ملاحظة جديدة | 💬 |
| رد جديد | عند إضافة رد على ملاحظة | ↩️ |
| حل ملاحظة | عند حل ملاحظة | ✅ |
| مرفقات جديدة | عند إضافة مرفقات | 📎 |

### أزرار سريعة:

- **تفعيل الكل**: يفعّل كل أنواع الإشعارات
- **إلغاء الكل**: يعطّل كل أنواع الإشعارات

### مثال عملي:

```
مصمم جرافيك يريد:
✅ مشاريع جديدة
✅ تحديثات الحالة
✅ ملاحظات جديدة
❌ ردود على الملاحظات (معطّل - مش مهتم)
❌ مرفقات جديدة (معطّل - مش مهتم)
```

---

## 🎯 أمثلة عملية

### مثال 1: مشروع جديد

**الإجراء:** تم إضافة مشروع "بطاقات عمل" للعميل "شركة ABC"

**من يستلم الإشعار:**
- ✅ الإداري (للمتابعة)
- ✅ المصمم (للبدء في التصميم)
- ❌ منسق المطبوعات (ليس وقته بعد)

---

### مثال 2: تغيير الحالة إلى "يوجد ملاحظات"

**الإجراء:** المراجع غيّر الحالة من "في انتظار المراجعة" إلى "ملاحظات العميل"

**من يستلم الإشعار:**
- ✅ المصمم (محتاج يعدل)
- ✅ الإداري (للمتابعة)
- ❌ منسق المطبوعات (مش relevant)
- ❌ المراجع نفسه (هو اللي عمل التغيير)

---

### مثال 3: تغيير الحالة إلى "جاري الطباعة"

**الإجراء:** منسق المطبوعات غيّر الحالة من "تم المونتاج" إلى "جاري الطباعة"

**من يستلم الإشعار:**
- ✅ الإداري (للمتابعة)
- ❌ المصمم (التصميم خلص)
- ❌ منسق المطبوعات (هو اللي عمل التغيير)

---

### مثال 4: ملاحظة جديدة

**الإجراء:** العميل كتب ملاحظة "يرجى تعديل اللون"

**من يستلم الإشعار:**
- ✅ المصمم (محتاج يشوف الملاحظة)
- ✅ الإداري (للمتابعة)
- ❌ منسق المطبوعات (مش relevant)

---

## 🔧 الإعدادات التقنية

### Backend Configuration

في ملف `backend/notifications/service.py`:

```python
ROLE_NOTIFICATION_PREFERENCES = {
    'designer': [
        'NEW_PROJECT',
        'NEW_SUBTASK',
        'STATUS_CHANGE',
        'COMMENT_ADDED',
        'REPLY_ADDED',
        'COMMENT_RESOLVED',
        'ATTACHMENT_ADDED'
    ],
    'print_manager': [
        'STATUS_CHANGE',
        'SUBTASK_UPDATE',
        'SUBTASK_SPECS_UPDATE',
        'COMMENT_ADDED',
        'ATTACHMENT_ADDED'
    ],
    'admin': [
        'NEW_PROJECT',
        'NEW_SUBTASK',
        'SUBTASK_UPDATE',
        'STATUS_CHANGE',
        'COMMENT_ADDED',
        'ATTACHMENT_ADDED'
    ]
}

ROLE_RELEVANT_STATUSES = {
    'designer': [
        'Has Comments',
        'Awaiting Materials',
        'On Hold',
        'Cancelled'
    ],
    'print_manager': [
        'Design Completed',
        'Ready for Montage',
        'In Montage',
        'Montage Completed',
        'In Printing',
        'Ready for Delivery'
    ],
    'admin': [
        'Ready for Delivery',
        'Delivered',
        'Cancelled',
        'On Hold'
    ]
}
```

### Smart Filtering Logic

#### 1. Role-Based Filtering

```python
def should_send_to_role(template_type, role, context):
    """Check if notification should be sent to this role"""
    
    # Check if template type is relevant to role
    if template_type not in ROLE_NOTIFICATION_PREFERENCES[role]:
        return False
    
    # Additional filtering for STATUS_CHANGE
    if template_type == 'STATUS_CHANGE':
        new_status = context.get('newStatus')
        return new_status in ROLE_RELEVANT_STATUSES[role]
    
    return True
```

#### 2. Action Creator Exclusion

```python
def should_exclude_action_creator(recipient, context):
    """Prevent sending notifications to the person who performed the action"""
    
    # Check by user ID
    if context.get('created_by_user_id') == recipient.get('userId'):
        return True
    
    # Check by phone number
    if context.get('created_by_phone') == recipient.get('phone'):
        return True
    
    return False
```

#### 3. User Preferences Check

```python
def check_user_preferences(recipient, template_type):
    """Check if user has enabled this notification type"""
    
    preferences = recipient.get('preferences', {})
    
    # If no preferences, send all (default: True)
    if not preferences:
        return True
    
    # Check if this type is enabled
    return preferences.get(template_type, True)
```

### Frontend Data Structure

```typescript
interface WhatsAppNumber {
    id: string;
    name: string;
    number: string;
    apiKey: string;
    role: 'admin' | 'designer' | 'print_manager';
    userId?: string;  // For action tracking
    enabled: boolean;
    preferences?: {
        NEW_PROJECT?: boolean;
        NEW_SUBTASK?: boolean;
        SUBTASK_UPDATE?: boolean;
        SUBTASK_SPECS_UPDATE?: boolean;
        STATUS_CHANGE?: boolean;
        COMMENT_ADDED?: boolean;
        REPLY_ADDED?: boolean;
        COMMENT_RESOLVED?: boolean;
        ATTACHMENT_ADDED?: boolean;
    }
}
```

---

## 🔍 نصائح مهمة

### 1. اختيار الدور المناسب
- اختر الدور بناءً على المسؤوليات الفعلية
- لو الشخص يقوم بأكثر من دور، اختر "إداري" ليستلم كل الإشعارات

### 2. تجنب الإشعارات المزعجة
- النظام تلقائياً يمنع إرسال إشعارات للشخص عن إجراءاته الخاصة
- مثال: المصمم لن يستلم إشعار عن تصميم قام به

### 3. اختبار الإشعارات
- استخدم زر "تجربة الإرسال" للتأكد من عمل الرقم
- تأكد من صحة API Key

### 4. تفعيل/تعطيل الأرقام
- يمكنك تعطيل رقم مؤقتاً بدون حذفه
- مفيد في حالة الإجازات أو الصيانة

---

## ❓ الأسئلة الشائعة

### س: هل يمكن إضافة أكثر من رقم لنفس الشخص؟
**ج:** نعم، يمكنك إضافة أكثر من رقم بأدوار مختلفة.

### س: ماذا لو لم أحدد دور وظيفي؟
**ج:** سيتم اعتباره "إداري" افتراضياً ويستلم كل الإشعارات.

### س: هل يمكن تغيير الدور بعد الإضافة؟
**ج:** حالياً لا، لكن يمكنك حذف الرقم وإضافته مرة أخرى بدور جديد.

### س: هل يمكن تخصيص الإشعارات لكل رقم؟
**ج:** نعم! اضغط على أيقونة الإعدادات (⚙️) بجانب أي رقم لتخصيص الإشعارات.

### س: ماذا يحدث إذا عطّلت كل الإشعارات؟
**ج:** لن يستلم هذا الرقم أي إشعارات، لكن يمكنك إعادة تفعيلها في أي وقت.

### س: لماذا لا أستلم بعض الإشعارات؟
**ج:** تحقق من:
1. الدور الوظيفي المحدد
2. أن الرقم مفعّل
3. أن الإشعار relevant لدورك
4. أنك لست من قام بالإجراء
5. تفضيلاتك الشخصية للإشعارات

### س: كيف أحصل على API Key من CallMeBot؟
**ج:** أضف رقم CallMeBot (+34 644 51 44 84) وأرسل: `I allow callmebot to send me messages`

---

## 🚀 التحسينات المستقبلية

### 1. Priority Levels
تصنيف الإشعارات حسب الأولوية:
- 🚨 عاجل (Urgent) - مهام عاجلة، مشاكل في الجودة
- ⚠️ مهم (Important) - ملاحظات جديدة، تغيير حالة
- ℹ️ عادي (Normal) - مرفقات، ردود

### 2. Notification Grouping
تجميع الإشعارات المتشابهة:
```
بدلاً من:
- تم إضافة ملاحظة على بطاقات عمل
- تم إضافة ملاحظة على بروشور
- تم إضافة ملاحظة على فلاير

يصبح:
- تم إضافة 3 ملاحظات جديدة
```

### 3. Scheduled Notifications
إرسال ملخص يومي بدلاً من إشعارات فورية:
```
📊 ملخص اليوم:
- 5 مشاريع جديدة
- 12 ملاحظة جديدة
- 8 مهام تم تسليمها
```

### 4. Notification History
حفظ سجل الإشعارات المرسلة:
- متى تم الإرسال
- لمن تم الإرسال
- هل تم التسليم بنجاح

---

## 📞 الدعم الفني

إذا واجهت أي مشكلة:
1. تحقق من صحة رقم الواتساب وAPI Key
2. جرب زر "تجربة الإرسال"
3. تأكد من اختيار الدور المناسب
4. راجع سجل الأنشطة (Activity Log)
5. تحقق من تفضيلات الإشعارات الشخصية

---

**آخر تحديث:** ديسمبر 2024  
**الإصدار:** 3.0.0
