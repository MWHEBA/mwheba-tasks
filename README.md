# MWHEBA Tasks - نظام إدارة المهام

نظام إدارة مهام متكامل مصمم خصيصاً لمؤسسة موهبة للحلول الدعائية (MWHEBA Agency)، مبني بتقنيات حديثة لتوفير تجربة سلسة وفعالة في إدارة المشاريع والمطبوعات.

## 🚀 المميزات الرئيسية

- **إدارة المهام**: تنظيم هرمي للمهام الرئيسية والفرعية مع تتبع التقدم
- **مواعيد التسليم الذكية**: تنبيهات بصرية تلقائية حسب الوقت المتبقي
- **مستويات الأولوية**: ثلاثة مستويات (عادي، عاجل، عاجل جداً)
- **فلاتر سريعة**: تصفية المهام حسب الحالة والعميل والأولوية
- **سجل النشاط**: تتبع شامل لجميع التغييرات والتحديثات
- **معاينة المرفقات**: معاينة الصور وملفات PDF مباشرة
- **إدارة العملاء**: قاعدة بيانات شاملة للعملاء
- **إدارة المنتجات**: كتالوج المنتجات والخدمات
- **حالات مخصصة**: إنشاء وتخصيص حالات المهام
- **إشعارات WhatsApp**: تكامل مع WhatsApp للإشعارات
- **واجهة عربية كاملة**: دعم كامل للغة العربية (RTL)

## 🛠️ التقنيات المستخدمة

### Frontend
- **React**: مكتبة JavaScript لبناء واجهات المستخدم
- **TypeScript**: للكتابة الآمنة والموثوقة
- **Vite**: أداة بناء سريعة وحديثة
- **Framer Motion**: حركات وانتقالات سلسة
- **React Router**: التنقل بين الصفحات
- **Tailwind CSS**: تصميم عصري ومرن

### Backend
- **Django**: إطار عمل Python قوي وآمن
- **Django REST Framework**: لبناء REST API
- **MySQL**: قاعدة بيانات موثوقة
- **Gunicorn**: WSGI server للإنتاج
- **Nginx**: Reverse proxy وخادم ويب

## 📦 التثبيت والتشغيل

### المتطلبات الأساسية

#### Frontend
- Node.js
- npm أو yarn

#### Backend
- Python 3.10+
- MySQL
- pip و virtualenv

### خطوات التثبيت

#### 1. Backend Setup

```bash
# الانتقال إلى مجلد backend
cd backend

# إنشاء بيئة افتراضية
python -m venv venv

# تفعيل البيئة الافتراضية
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# تثبيت المكتبات
pip install -r requirements.txt

# تكوين المتغيرات البيئية
cp .env.example .env
# عدل ملف .env بمعلومات قاعدة البيانات

# تشغيل migrations
python manage.py migrate --fake-initial

# تشغيل الخادم
python manage.py runserver
```

Backend سيعمل على: `http://localhost:8000`

#### 2. Frontend Setup

```bash
# في المجلد الرئيسي
npm install

# تشغيل التطبيق
npm run dev
```

Frontend سيعمل على: `http://localhost:5173`

### أوامر إضافية

```bash
# Frontend
npm run build          # بناء للإنتاج
npm run preview        # معاينة النسخة المبنية

# Backend
cd backend
python manage.py test  # تشغيل الاختبارات
pytest                 # تشغيل اختبارات pytest
python manage.py collectstatic  # جمع الملفات الثابتة
```

### للنشر في الإنتاج

راجع الأدلة التفصيلية:
- **دليل النشر**: [docs/DEPLOYMENT_GUIDE.md](docs/DEPLOYMENT_GUIDE.md) - دليل مبسط وسريع
- **Backend**: [backend/README.md](backend/README.md) - دليل شامل للـ Backend

## 📚 الوثائق

للحصول على معلومات تفصيلية، راجع:
- **[دليل الوثائق](docs/README.md)** - فهرس شامل لجميع الوثائق
- **[دليل النشر](docs/DEPLOYMENT_GUIDE.md)** - نشر التحديثات
- **[دليل الإشعارات](docs/NOTIFICATIONS_GUIDE.md)** - نظام الإشعارات الذكي
- **[Backend README](backend/README.md)** - دليل Backend الشامل

---

## 📖 دليل الاستخدام

### إنشاء مهمة جديدة

1. انقر على زر "مهمة جديدة" في الصفحة الرئيسية
2. املأ التفاصيل:
   - العنوان والوصف
   - اختر العميل
   - حدد مستوى الأولوية (عادي/عاجل/عاجل جداً)
   - حدد موعد التسليم (اختياري)
   - أضف المرفقات إذا لزم الأمر
3. انقر "حفظ"

### استخدام الفلاتر السريعة

1. في صفحة قائمة المهام، ستجد شريط الفلاتر في الأعلى
2. انقر على:
   - **المتأخرة**: لعرض المهام المتأخرة فقط
   - **العاجلة**: لعرض المهام العاجلة والعاجلة جداً
   - **حسب العميل**: لاختيار عميل معين
3. انقر "إلغاء الفلتر" للعودة لعرض جميع المهام

### معاينة المرفقات

1. في صفحة تفاصيل المهمة، انقر على أي مرفق
2. ستفتح نافذة المعاينة:
   - للصور: استخدم أزرار +/- للتكبير والتصغير
   - للصور المكبرة: اسحب بالماوس للتحرك
   - للمرفقات المتعددة: استخدم الأسهم للتنقل
3. انقر زر التحميل لحفظ الملف
4. اضغط ESC أو زر الإغلاق للخروج

### مراقبة التقدم

- في قائمة المهام، ستجد شريط التقدم تحت كل مهمة رئيسية
- يعرض الشريط:
  - عدد المهام المنتهية من الإجمالي
  - النسبة المئوية
  - لون يعكس مستوى الإنجاز

### مراجعة سجل النشاط

1. افتح تفاصيل أي مهمة
2. انتقل إلى قسم "سجل النشاط"
3. ستجد جميع التغييرات مرتبة زمنياً
4. انقر "عرض المزيد" لرؤية الإدخالات القديمة

## 🎯 حالات الاستخدام

### للمديرين
- تتبع مواعيد التسليم وتجنب التأخير
- مراقبة تقدم المشاريع بسرعة
- فلترة المهام حسب الأولوية والعميل
- مراجعة سجل النشاط لحل المشاكل

### للمصممين
- معرفة المهام العاجلة بسرعة
- معاينة المرفقات دون تحميل
- تتبع التعليقات والتحديثات
- رؤية واضحة للمهام المتبقية

### لمديري الطباعة
- فلترة المهام حسب نوع الطباعة
- تتبع المهام المتأخرة
- إدارة أولويات الإنتاج
- التواصل عبر التعليقات

## 🔧 البنية التقنية

### هيكل المشروع

```
mwheba-tasks/
├── components/          # المكونات القابلة لإعادة الاستخدام
│   ├── DeadlineBadge.tsx      # عرض موعد التسليم
│   ├── ProgressBar.tsx        # شريط التقدم
│   ├── QuickFilters.tsx       # الفلاتر السريعة
│   ├── ActivityLog.tsx        # سجل النشاط
│   ├── AttachmentPreview.tsx  # معاينة المرفقات
│   └── ...
├── pages/              # صفحات التطبيق
│   ├── TaskList.tsx           # قائمة المهام
│   ├── TaskDetail.tsx         # تفاصيل المهمة
│   ├── CreateTask.tsx         # إنشاء مهمة
│   └── ...
├── services/           # طبقة البيانات والمنطق
│   ├── taskService.ts         # خدمات المهام
│   ├── clientService.ts       # خدمات العملاء
│   └── ...
├── utils/              # وظائف مساعدة
│   ├── dateUtils.ts           # وظائف التواريخ
│   └── filterUtils.ts         # وظائف الفلترة
├── types.ts            # تعريفات TypeScript
└── App.tsx             # المكون الرئيسي
```

### نماذج البيانات الرئيسية

#### Task (المهمة)
```typescript
interface Task {
  id: string;
  title: string;
  description: string;
  urgency: Urgency;              // عادي | عاجل | عاجل جداً
  status: string;
  clientId: string;
  deadline?: number;             // موعد التسليم
  activityLog?: ActivityLogEntry[]; // سجل النشاط
  // ... حقول أخرى
}
```

#### ActivityLogEntry (إدخال سجل النشاط)
```typescript
interface ActivityLogEntry {
  id: string;
  timestamp: number;
  type: ActivityType;            // نوع النشاط
  description: string;           // وصف النشاط
  details?: Record<string, any>; // تفاصيل إضافية
}
```

## 🎨 إمكانية الوصول (Accessibility)

التطبيق مصمم ليكون متاحاً للجميع:

- ✅ دعم كامل للوحة المفاتيح
- ✅ ARIA labels واضحة
- ✅ تباين ألوان مناسب
- ✅ Focus indicators واضحة
- ✅ دعم قارئات الشاشة
- ✅ تصميم متجاوب (Responsive)

### اختصارات لوحة المفاتيح

في معاينة المرفقات:
- `ESC`: إغلاق المعاينة
- `←`: المرفق التالي
- `→`: المرفق السابق
- `+` أو `=`: تكبير
- `-`: تصغير
- `0`: إعادة تعيين التكبير

في الفلاتر:
- `Tab`: التنقل بين الفلاتر
- `Enter` أو `Space`: تفعيل الفلتر
- `Escape`: إغلاق قائمة العملاء

## 🚀 الأداء والتحسينات

- استخدام `useMemo` لتحسين حسابات التقدم والفلترة
- `React.lazy` لتحميل مكون المعاينة عند الحاجة فقط
- `useCallback` لتحسين إعادة الرسم
- تحميل تدريجي لسجل النشاط (10 إدخالات أولاً)
- تخزين محلي فعال مع LocalStorage

## 📝 التوثيق الفني

### وظائف الخدمات

#### TaskService

```typescript
/**
 * تحديد موعد تسليم لمهمة
 * @param taskId - معرف المهمة
 * @param deadline - موعد التسليم (timestamp)
 */
setDeadline(taskId: string, deadline: number): Promise<void>

/**
 * الحصول على المهام المتأخرة
 * @returns قائمة المهام التي تجاوزت موعد التسليم
 */
getOverdueTasks(): Promise<Task[]>

/**
 * الحصول على المهام العاجلة
 * @returns المهام ذات الأولوية العاجلة أو العاجلة جداً
 */
getUrgentTasks(): Promise<Task[]>

/**
 * الحصول على مهام عميل معين
 * @param clientId - معرف العميل
 * @returns قائمة مهام العميل
 */
getTasksByClient(clientId: string): Promise<Task[]>

/**
 * إضافة إدخال لسجل النشاط
 * @param taskId - معرف المهمة
 * @param entry - بيانات الإدخال
 */
addActivityLog(taskId: string, entry: Omit<ActivityLogEntry, 'id' | 'timestamp'>): Promise<void>

/**
 * حساب تقدم المهام الفرعية
 * @param taskId - معرف المهمة الرئيسية
 * @returns عدد المهام المنتهية والإجمالي والنسبة المئوية
 */
calculateProgress(taskId: string): Promise<{ completed: number; total: number; percentage: number }>
```

## 🤝 المساهمة

نرحب بالمساهمات! إذا كنت ترغب في تحسين التطبيق:

1. Fork المشروع
2. أنشئ فرع للميزة الجديدة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT.

## 📞 الدعم

للأسئلة والدعم، يرجى فتح Issue في المستودع.

---

صُنع بـ ❤️ للوكالات الإبداعية والمطابع
