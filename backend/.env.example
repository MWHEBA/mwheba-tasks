# ============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ============================================================================
# SECURITY WARNING: This file contains sensitive data - never commit to git!
# ============================================================================

# DJANGO CORE SETTINGS
# DEBUG must be False in production to prevent information leakage
DEBUG=False

# SECRET_KEY - Cryptographically secure key for Django
# Generate using: python -c "import secrets; print(secrets.token_urlsafe(64))"
# SECURITY: This key is unique and must be kept secret
SECRET_KEY=your-secret-key-here

# ALLOWED_HOSTS - Domains that Django can serve
# SECURITY: Prevents HTTP Host header attacks
ALLOWED_HOSTS=your-domain.com,localhost,127.0.0.1

# DATABASE CONFIGURATION
# MySQL database credentials for production
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_HOST=localhost
DB_PORT=3306

# CORS CONFIGURATION
# Production and development frontend URLs
# SECURITY: Development URLs should be removed in production
CORS_ALLOWED_ORIGINS=https://your-domain.com,http://localhost:5173
